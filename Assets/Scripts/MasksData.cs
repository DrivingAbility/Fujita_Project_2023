using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MasksData : MonoBehaviour
{
    [SerializeField] Transform _stencilMask;
    public Transform StencilMask => _stencilMask;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
